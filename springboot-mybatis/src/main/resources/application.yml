server:
  port: 8080

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.seamew.domain
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/learn_springboot?charsetEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ltr20001121

logging:
  level:
    root: info
